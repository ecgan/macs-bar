title: Macs Bar
description: A lightweight macOS window taskbar that tracks and switches between open windows across desktop Spaces.
baseurl: "" # Keep empty if deploying to a custom domain or organization page root
url: "https://ecgan.github.io/macs-bar"
github_username: ecgan

# Build settings
markdown: kramdown
theme: null # Custom premium styling
exclude:
  - Gemfile
  - Gemfile.lock
  - vendor/
